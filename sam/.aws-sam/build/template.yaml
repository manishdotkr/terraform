AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Resources:
  MyLambdaFunction:
    Type: AWS::Serverless::Function
    Properties:
      PackageType: Image
      ImageConfig:
        Command:
        - index.handler
      MemorySize: 512
      Timeout: 900
      Environment:
        Variables:
          DATABASE_HOST: testing-db.cneyassm2suc.us-east-2.rds.amazonaws.com
          DATABASE_NAME: legaltech-prod2
          DATABASE_USERNAME: postgres
          DATABASE_PASSWORD: password
          RDS_BACKUP_S3_BUCKET: lth-backup
          ENVIRONMENT: dev
      ImageUri: mylambdafunction:latest
    Metadata:
      DockerContext: /home/manish.kumar201/lth/tasks/13. rds backup to s3/lambda/rds-to-s3-backup
      Dockerfile: Dockerfile
      SamResourceId: MyLambdaFunction
